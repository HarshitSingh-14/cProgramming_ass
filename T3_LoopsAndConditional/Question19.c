#include <stdio.h>
#include <math.h>

int main(){

int a = -12, b=4,c=99;
if (a>b){ if (b<c){printf("b is the smallest");}

else {printf("c is the smallest");}
 }

 else if (c>a) printf("a is the smallest");

return 0;
}
